interface:
  display_name: "Grill Me"
  short_description: "Pressure-test a plan one decision at a time"
  default_prompt: "Use $grill-me to pressure-test this plan one decision at a time."
policy:
  allow_implicit_invocation: false
